define( './lib/util', function ( require, module, exports ) {
    var dom = require( './lib/dom/' );
    var ajax = require( './lib/ajax/' );
    module.exports = {
        name: './lib/util'
    };
} );
