define( './lib/util', function ( require, exports, module ) {
    var dom = require( './lib/dom/' );
    var ajax = require( './lib/ajax/' );
    module.exports = {
        name: './lib/util'
    };
} );
